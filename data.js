// data.js — storage helpers only
// NOTE: app.js manages all state via localStorage keys prefixed with KEY ('rb5_')
// This file exists for compatibility — does NOT maintain a parallel state object.

function saveState(){
  // no-op: app.js handles all persistence directly via save() calls
}
